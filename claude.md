# Project Guidelines

- **No Suppressions**: Do not use `// eslint-disable`, `// @ts-ignore`, `// @ts-expect-error` or similar suppressions. All issues must be fixed properly with correct types or code changes.
- **Package Manager**: Use `pnpm` for package management. Do not use `npm` or `yarn`.
